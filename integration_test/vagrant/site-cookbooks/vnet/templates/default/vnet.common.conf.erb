registry {
  adapter "redis"
  host "<%= @registry_host %>"
  port <%= @registry_port %>
}

db {
  adapter "mysql2"
  database "vnet"
  host "<%= @db_host %>"
  port <%= @db_port %>
  user "root"
  password ""
}
