# -*- coding: utf-8 -*-
require 'spec_helper'

describe Vnet::NodeModules::EventHandler do

  let!(:datapath_1) { Fabricate(:datapath_1) }

  before(:each) do
  end

  describe "handle_event" do
    it "handle correctly" do
      options = {:bbb => 1, :ccc => 2}
      node = double(:node)
      actor = double(:actor)

      expect(node).to receive(:[]).with(:service_openflow).and_return(actor)
      DCell::Node.stub(:[]).with("vna").and_return(node)
      expect(actor).to receive(:async).and_return(actor)
      expect(actor).to receive(:handle_event).with(:aaa, options)

      Vnet::NodeModules::EventHandler.new.handle_event(:aaa, options)
    end
  end
end
